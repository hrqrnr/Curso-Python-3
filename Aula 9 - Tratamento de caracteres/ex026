#numa frase:
#quantidade de aparições letra "a"
#a primeira
#última

frase = str(input('Digite uma frase: \n')).lower().strip()

quantidade = frase.count('a')
primeira = frase.find('a')+1
última = frase.rfind('a')+1
#IMPORTANTE DMS EM PYTHON: lembrar q a contagem de strings começa no 0, logo quando quiser mostrar algo no meio dela é necessário add +1, como nos dois casos acima

print('Quantidade: {}'.format(quantidade))
print('Primeira: {}'.format(primeira))
print('Última: {}'.format(última))
